export function TestEventTimeline({events}:{events:Array<{created_at:string;payload:unknown}>}){return <ol className="space-y-3 border-l pl-4">{events.map((event,index)=><li key={index}><p className="text-xs text-muted">{new Date(event.created_at).toLocaleString()}</p><pre className="mt-1 overflow-auto rounded-xl bg-charcoal p-3 text-xs text-white">{JSON.stringify(event.payload,null,2)}</pre></li>)}</ol>}
