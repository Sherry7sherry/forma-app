'use client'
import { useState } from 'react'
import { ReportIssueSheet } from './ReportIssueSheet'
export function InternalTestOverlay({movement,phase,notice,onRecord,onRetry,onForceContinue,onEnd}:{movement:string;phase:string;notice?:string;onRecord(issue:unknown):Promise<void>|void;onRetry():void;onForceContinue():Promise<void>|void;onEnd():void}){const[open,setOpen]=useState(true);const[actionNotice,setActionNotice]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function runAction(action:()=>Promise<void>|void,success:string){setBusy(true);setActionNotice('Saving internal test evidence…');try{await action();setActionNotice(success)}catch(error){setActionNotice(`Could not record: ${error instanceof Error?error.message:'unknown error'}`)}finally{setBusy(false)}}return <aside className="pointer-events-auto fixed bottom-3 right-3 z-[100] w-[min(22rem,calc(100vw-1.5rem))] rounded-2xl bg-slate-950 p-3 text-white shadow-2xl"><button type="button" onClick={()=>setOpen(x=>!x)} className="w-full text-left text-xs font-bold uppercase tracking-wider">Internal test · {movement} · {phase}</button>{open&&<div className="mt-3 grid gap-2"><p className="text-xs text-white/60">Build {process.env.NEXT_PUBLIC_BUILD_VERSION??'local'} · Profile {process.env.NEXT_PUBLIC_TRACKING_PROFILE_VERSION??'1'}</p>{(actionNotice??notice)&&<p role="status" aria-live="polite" className="rounded-xl bg-white/10 p-2 text-xs text-white">{actionNotice??notice}</p>}<ReportIssueSheet onSubmit={issue=>runAction(()=>onRecord(issue),'Problem recorded. You can retry or record + continue.')} /><div className="grid grid-cols-2 gap-2"><button type="button" disabled={busy} onClick={onRetry} className="rounded-xl bg-white/10 p-2 text-sm disabled:opacity-50">Retry phase</button><button type="button" disabled={busy} onClick={()=>runAction(onForceContinue,'Synthetic continuation recorded. Production data was not changed.')} className="rounded-xl bg-amber-500 p-2 text-sm text-black disabled:opacity-50">Record + continue</button></div><button type="button" onClick={onEnd} className="text-xs text-white/60">End test run</button></div>}</aside>}
