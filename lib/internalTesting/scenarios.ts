import { getTestableMovement } from '@/lib/internalTesting/movementRegistry'
export interface TestScenario { movementId:string; phase:string; scenarioId:string; repeats:number }
export function serializeTestScenario(value:TestScenario){const params=new URLSearchParams({v:'1',movement:value.movementId,phase:value.phase,scenario:value.scenarioId,repeats:String(value.repeats)});return params}
export function parseTestScenario(params:URLSearchParams):TestScenario { if(params.get('v')!=='1')throw new TypeError('unsupported scenario version'); const movementId=params.get('movement')??'';const movement=getTestableMovement(movementId);if(!movement)throw new TypeError('invalid movement');const phase=params.get('phase')??'';if(!['setup','capture','calibrating','exercising'].includes(phase))throw new TypeError('invalid phase');const scenarioId=params.get('scenario')??'';if(!movement.scenarios.some(item=>item.id===scenarioId))throw new TypeError('incompatible scenario');const repeats=Number(params.get('repeats'));if(!Number.isInteger(repeats)||repeats<1||repeats>20)throw new TypeError('invalid repeat count');return{movementId,phase,scenarioId,repeats} }
